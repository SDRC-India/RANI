import { PerformanceDashboardPipe } from './performance-dashboard.pipe';

describe('PerformanceDashboardPipe', () => {
  it('create an instance', () => {
    const pipe = new PerformanceDashboardPipe();
    expect(pipe).toBeTruthy();
  });
});
