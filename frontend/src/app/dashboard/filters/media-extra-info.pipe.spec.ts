import { MediaExtraInfoPipe } from './media-extra-info.pipe';

describe('MediaExtraInfoPipe', () => {
  it('create an instance', () => {
    const pipe = new MediaExtraInfoPipe();
    expect(pipe).toBeTruthy();
  });
});
