import { ClusterAreaPipe } from './cluster-area.pipe';

describe('ClusterAreaPipe', () => {
  it('create an instance', () => {
    const pipe = new ClusterAreaPipe();
    expect(pipe).toBeTruthy();
  });
});
