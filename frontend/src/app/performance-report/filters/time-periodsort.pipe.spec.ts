import { TimePeriodsortPipe } from './time-periodsort.pipe';

describe('TimePeriodsortPipe', () => {
  it('create an instance', () => {
    const pipe = new TimePeriodsortPipe();
    expect(pipe).toBeTruthy();
  });
});
