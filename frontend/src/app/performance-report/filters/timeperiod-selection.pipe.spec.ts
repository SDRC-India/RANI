import { TimeperiodSelectionPipe } from './timeperiod-selection.pipe';

describe('TimeperiodSelectionPipe', () => {
  it('create an instance', () => {
    const pipe = new TimeperiodSelectionPipe();
    expect(pipe).toBeTruthy();
  });
});
